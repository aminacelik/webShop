<h2><%= @product.title %></h2>



<%= form_for(@product_variant) do |f| %>
  <% if @product_variant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_variant.errors.count, "error") %> prohibited this product_variant from being saved:</h2>

      <ul>
      <% @product_variant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<table>
	
	 <tr>
	  	<td><%= f.label :size %></td>
	  	<td><%= f.collection_select :size_id, Size.all, :id, :size %></td>
  	</tr>
	
	 <tr>
	  	<td><%= f.label :color %></td>
	  	<td><%= f.collection_select :color_id, Color.all, :id, :name %></td>
  	</tr> 
	<tr>
		<td><%= f.label :quantity %></td>
    	<td><%= f.text_field :quantity %></td>
	</tr>
	<%= hidden_field_tag :product_id, @product.id %>
</table>
<%= f.submit 'Create', class: 'button' %>


<% end %>
