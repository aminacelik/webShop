<h2><%= @product.title %></h2>



<%= form_for(@product_variant, html: {role: 'form', class: 'col-xs-12 col-sm-6'}) do |f| %>
  <% if @product_variant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_variant.errors.count, "error") %> prohibited this product_variant from being saved:</h2>

      <ul>
      <% @product_variant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="form-group">
	  	<%= f.label :size %>
	  	<%= f.collection_select :size_id, Size.all, :id, :size %>
  </div>

  <div class="form-group">
	  	<%= f.label :color %>
	  	<%= f.collection_select :color_id, Color.all, :id, :name %>
  </div>

	<div class="form-group">
		  <%= f.label :quantity %>
    	<%= f.text_field :quantity %>
  </div>
	
	<%= hidden_field_tag :product_id, @product.id %>

  <%= f.submit 'Create', class: 'btn btn-default grey_button' %>


<% end %>
