<%= form_for(@product_translation) do |f| %>
  <% if @product_translation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_translation.errors.count, "error") %> prohibited this product_translation from being saved:</h2>

      <ul>
      <% @product_translation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :language_id %><br>
    <%= f.collection_select :language_id, @languages, :id, :name %><br>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.collection_select :product_id, @products, :id, :title %><br>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
